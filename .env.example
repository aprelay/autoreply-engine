# Token Sender Express - Environment Variables
# Copy this file to .env and customize

# Server port (Railway auto-sets PORT)
PORT=3000

# SQLite database path (Railway provides persistent volume at /app/data)
DB_PATH=/app/data/token-sender.db

# Node environment
NODE_ENV=production
